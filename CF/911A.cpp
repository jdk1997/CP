#include<bits/stdc++.h>
using namespace std;
int main()
{
	ios::sync_with_stdio(false);
	int n = 0;
	cin>>n;
	int arr[n] = {0};
	for(int i = 0; i < n; ++i)
	{
		cin>>arr[i];
	}
	long long min = arr[0];
	for(int j = 0; j < n; ++j)
	{
		if(arr[j] < min)
		{
			min = arr[j];
			po
		}
	}
	return 0;
}
